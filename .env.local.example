# Instructions:
# 1. Go to https://supabase.com/dashboard/project/your-project-id/settings/api
# 2. Copy the "anon public" key (starts with "eyJ...")
# 3. Replace YOUR_ANON_KEY_HERE with your actual key
# 4. Never commit this file to git (it's in .gitignore)

# ========================================
# Supabase Local Development
# ========================================
# Ces variables sont utilisées par les scripts de développement local.
#? Pour obtenir ces valeurs, exécutez: supabase status

# Local Supabase API URL (par défaut pour toutes les instances)
SUPABASE_LOCAL_URL=http://127.0.0.1:78654

# Local Supabase Publishable Key (anon)
#TODO : supabase status | grep "anon key"
# Note: Clé par défaut identique pour toutes les instances Supabase locales
SUPABASE_LOCAL_PUBLISHABLE_KEY=sb_publishable_FESKRzQHlZjBrEyhHvfOze_7BJgxPraP

# Local Supabase Service Role Key
# TODO : supabase status | grep "service_role key"
#! ⚠️ ATTENTION: Cette clé confère des privilèges élevés (bypass RLS)
SUPABASE_LOCAL_SERVICE_KEY=sb_secret_J8JEURTgjKLMZ-Uofvd0Hg_xZvEOItr

# ========================================
# 🔒 SÉCURITÉ - Clés Locales Supabase
# ========================================
# CES CLÉS SONT UNIQUEMENT POUR LE DÉVELOPPEMENT LOCAL !
#
# Portée:
#   - Fonctionnent UNIQUEMENT sur localhost (127.0.0.1:78654)
#   - Identiques pour toutes les instances Supabase locales
#   - Obtenues via: pnpm dlx supabase status
#
# Risques:
#!   - Service Key: Bypass RLS, accès admin complet à la base locale
#   - Si exposées publiquement: accès non autorisé à votre DB locale
#
# Recommandations:
#   ✅ Gardez .env.local dans .gitignore (déjà fait)
#   ✅ Ne commitez JAMAIS ces clés dans le code
#   ✅ N'exposez JAMAIS localhost:78654 sur Internet (tunnel/ngrok)
#   ❌ NE PAS utiliser en production (utilisez les clés Supabase Cloud)
#
# Si compromises (commit accidentel, CI publique, tunnel):
#   1. Arrêter Supabase local: pnpm dlx supabase stop
#   2. Supprimer volumes Docker: pnpm dlx supabase db reset
#   3. Nettoyer l'historique Git si commitées
# ========================================

# ================================================================
#* 🌐 APPLICATION CONFIGURATION (Required)
# ================================================================
# Supabase Db url connection string for testing (used in test environment)
#* cloud Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
#* New Supabase Asymetric JWT
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_OR_ANON_KEY=your-anon-key
#! This key is safe to expose publicly but ONLY if you have Row Level Security (RLS) enabled on all tablesbase Configuration for Rouge-Cardinal-Companie

project_id=your-project-id
# ajouté pour test (manque variable d'environnement)
SUPABASE_PROJECT_REF=your-project-ref

# Dev mode URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ================================================================
#* 📧 EMAIL CONFIGURATION (Resend - Required)
# ================================================================

# DEVELOPMENT EMAIL REDIRECT
# When `EMAIL_DEV_REDIRECT` is `true` and you run the app in development,
# invitation emails will be redirected to `EMAIL_DEV_REDIRECT_TO`.
# This is intended for local testing with providers that restrict recipients (eg. Resend test-mode).

# Resend API Key (required for email sending)
RESEND_API_KEY=your_resend_api_key
# Email sender address (must be verified in Resend dashboard)
EMAIL_FROM=your_verified_email

# Contact email for admin notifications
# suppression list after a hard bounce ⚠️
EMAIL_CONTACT=your_admin_contact_email

# =============================================================
# 🚨 DEVELOPMENT EMAIL REDIRECT (DEV ONLY)
# =============================================================
#
# ⚠️  CRITICAL WARNING: This feature MUST be disabled in production!
#
# When enabled, ALL invitation emails will be redirected to EMAIL_DEV_REDIRECT_TO
# regardless of the recipient's actual email address.
#
# Purpose: Test invitation flow locally without sending real emails
# Activation: NODE_ENV='development' AND EMAIL_DEV_REDIRECT='true'
#
# ❌ DO NOT enable this in production - it will redirect real user invitations!
# ✅ Safe values for production: 'false', undefined, or omit entirely
#
EMAIL_DEV_REDIRECT=true
EMAIL_DEV_REDIRECT_TO=your_admin_contact_email

# Sentry Error Monitoring
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn
SENTRY_DSN=your_sentry_dsn

SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=your_sentry_project
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# ===============================================================
# 🤖 MCP TOOLS & CI/CD (Optional - External tooling only)
# =================================================================
# These variables are used by MCP Supabase tools and CI/CD pipelines.
# They are NOT required for the Next.js application to run.
# 
# Used by:
# - Claude MCP Supabase tools (database operations via MCP)
# - GitHub Actions workflows (database migrations, testing)
# - Supabase CLI commands in CI/CD
#
# Safe to omit if you're only running the Next.js app locally.

# GitHub token for actions that require authentication
GITHUB_TOKEN=your_github_token
SUPABASE_ACCESS_TOKEN=your-access-token
# EXTERNAL SERVICES
CONTEXT7_API_KEY=...

# ========================================
#*TODO ⚠️ Production Environment Variables ⚠️
# ========================================
#! Ces variables sont utilisées par l'application Next.js
#! NE PAS inclure les credentials de production ici !
# Utilisez Vercel/votre plateforme de déploiement pour les secrets
