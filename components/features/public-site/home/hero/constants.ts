/** Intervalle entre les slides en mode auto-play (ms) */
export const AUTO_PLAY_INTERVAL_MS = 6_000;

/** Durée de pause après interaction utilisateur avant reprise auto-play (ms) */
export const PAUSE_AFTER_INTERACTION_MS = 10_000;

/** Distance minimale de swipe pour déclencher un changement de slide (px) */
export const MIN_SWIPE_DISTANCE_PX = 50;

