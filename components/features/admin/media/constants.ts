/** Maximum number of tags shown inline in MediaCard and BulkTagSelector */
export const MAX_VISIBLE_TAGS = 3;

/** Search debounce delay in milliseconds (MediaLibraryPicker) */
export const SEARCH_DEBOUNCE_MS = 300;

/** External URL validation debounce delay in milliseconds (ImageFieldGroup) */
export const URL_VALIDATION_DEBOUNCE_MS = 1000;

/** IntersectionObserver root margin for lazy image loading (MediaCard) */
export const INTERSECTION_OBSERVER_MARGIN = "50px";
